name: SiegePal Security
on:
  workflow_dispatch:
    inputs:
      semgrep:
        description: "Run Semgrep"
        type: boolean
        default: true
      trivy:
        description: "Run Trivy"
        type: boolean
        default: true
      checkov:
        description: "Run Checkov"
        type: boolean
        default: true
      bandit:
        description: "Run Bandit"
        type: boolean
        default: true
      snyk:
        description: "Run Snyk (SCA)"
        type: boolean
        default: true
      snyk_code:
        description: "Run Snyk Code"
        type: boolean
        default: true
      snyk_iac:
        description: "Run Snyk IaC"
        type: boolean
        default: true
  schedule:
    - cron: "17 4 * * 1"
permissions:
  contents: read
  security-events: write
  actions: read
  checks: write
jobs:
  semgrep:
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.semgrep == true || inputs.semgrep == 'true' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        ref: ${{ fromJSON(github.event_name != 'schedule' && format('["{0}"]', github.ref_name) || '["main"]') }}
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ matrix.ref }}
      - name: Resolve scanned ref
        id: resolve_ref
        run: |
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install Semgrep
        run: pip install --disable-pip-version-check --no-cache-dir semgrep
      - name: Run Semgrep
        id: scan
        continue-on-error: true
        run: semgrep --config auto --sarif --output semgrep.sarif .
      - name: Upload SARIF
        if: always()
        continue-on-error: true
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: semgrep.sarif
          ref: refs/heads/${{ matrix.ref }}
          sha: ${{ steps.resolve_ref.outputs.sha }}
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: siegepal-semgrep-results-${{ matrix.ref }}
          path: semgrep.sarif
      - name: SiegePal gate — evaluate findings
        id: siegepal_gate
        if: always()
        env:
          SIEGEPAL_GATE_SEVERITY: "high"
          SIEGEPAL_SARIF_FILE: "semgrep.sarif"
        run: |
          python3 - <<'PYEOF'
          import json, os
          order = ["none", "low", "medium", "high", "critical"]
          threshold = os.environ.get("SIEGEPAL_GATE_SEVERITY", "none").lower()
          sarif_file = os.environ.get("SIEGEPAL_SARIF_FILE", "")
          out = os.environ.get("GITHUB_OUTPUT")
          def emit(**kw):
              with open(out, "a") as fh:
                  for k, v in kw.items():
                      fh.write("%s=%s\n" % (k, v))
          def sev_from_result(r, rules):
              props = r.get("properties") or {}
              sc = props.get("security-severity")
              if sc is None:
                  rid = r.get("ruleId")
                  rule = rules.get(rid, {})
                  sc = ((rule.get("properties") or {}).get("security-severity"))
              if sc is not None:
                  try:
                      f = float(sc)
                  except (TypeError, ValueError):
                      f = -1
                  if f >= 9.0: return 4
                  if f >= 7.0: return 3
                  if f >= 4.0: return 2
                  if f > 0.0: return 1
              level = (r.get("level") or "").lower()
              return {"error": 3, "warning": 2, "note": 1, "none": 0}.get(level, 2)
          # A parseable SARIF file is the authoritative 'the scanner ran'
          # signal — most scanners exit non-zero when they FIND issues,
          # so the tool's exit code must NOT gate this (that would flag
          # every findings-present scan as an inconclusive crash).
          if not os.path.exists(sarif_file):
              # Missing output file is never "zero findings" (R5.5).
              emit(conclusion="neutral", findings="0",
                   summary="Tool did not produce an output file; result is inconclusive.")
              raise SystemExit(0)
          try:
              with open(sarif_file) as fh:
                  doc = json.load(fh)
          except Exception as exc:
              emit(conclusion="neutral", findings="0",
                   summary="Could not parse scanner output: %s" % exc)
              raise SystemExit(0)
          t = order.index(threshold) if threshold in order else 0
          count = 0
          for run in (doc.get("runs") or []):
              rules = {}
              for rule in (((run.get("tool") or {}).get("driver") or {}).get("rules") or []):
                  if rule.get("id"): rules[rule["id"]] = rule
              for res in (run.get("results") or []):
                  if sev_from_result(res, rules) >= t:
                      count += 1
          if count > 0:
              emit(conclusion="failure", findings=str(count),
                   summary="%d finding(s) at or above %s severity." % (count, threshold))
          else:
              emit(conclusion="success", findings="0",
                   summary="No findings at or above %s severity." % threshold)
          PYEOF
      - name: SiegePal gate — report check run
        if: always()
        continue-on-error: true
        uses: actions/github-script@v8
        env:
          SIEGEPAL_CHECK_CONCLUSION: "${{ steps.siegepal_gate.outputs.conclusion }}"
          SIEGEPAL_CHECK_SUMMARY: "${{ steps.siegepal_gate.outputs.summary }}"
          SIEGEPAL_HEAD_SHA: "${{ steps.resolve_ref.outputs.sha }}"
        with:
          script: |
            if (context.payload.pull_request && context.payload.pull_request.head.repo.fork) {
              core.info("Fork PR: GITHUB_TOKEN is read-only, skipping check run creation.");
              return;
            }
            const conclusion = process.env.SIEGEPAL_CHECK_CONCLUSION || "neutral";
            const advisory = 'This result is advisory. To block merges, mark this check ("SiegePal / Semgrep") as a required status check in Settings → Branches → Branch protection rules.';
            const summary = (process.env.SIEGEPAL_CHECK_SUMMARY || "") + "\n\n" + advisory;
            const headSha = process.env.SIEGEPAL_HEAD_SHA || (context.payload.pull_request && context.payload.pull_request.head.sha) || context.sha;
            await github.rest.checks.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              name: 'SiegePal / Semgrep',
              head_sha: headSha,
              status: "completed",
              conclusion: conclusion,
              output: { title: 'SiegePal / Semgrep', summary: summary },
            });
      - name: SiegePal gate — enforce
        if: always()
        env:
          SIEGEPAL_CHECK_CONCLUSION: "${{ steps.siegepal_gate.outputs.conclusion }}"
        run: |
          if [ "${{ github.event.pull_request.head.repo.fork }}" = "true" ]; then
            echo "Fork PR: findings uploaded as artifacts; not failing the workflow."
            exit 0
          fi
          if [ "$SIEGEPAL_CHECK_CONCLUSION" = "failure" ]; then
            echo "SiegePal gate: blocking findings present."
            exit 1
          fi
          if [ "$SIEGEPAL_CHECK_CONCLUSION" = "neutral" ]; then
            echo "SiegePal gate: no conclusive output (nothing to scan or tool produced no report); not blocking."
            exit 0
          fi
          echo "SiegePal gate: passed."
  trivy:
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.trivy == true || inputs.trivy == 'true' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        ref: ${{ fromJSON(github.event_name != 'schedule' && format('["{0}"]', github.ref_name) || '["main"]') }}
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ matrix.ref }}
      - name: Resolve scanned ref
        id: resolve_ref
        run: |
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
      - name: Run Trivy
        id: scan
        continue-on-error: true
        run: >
          docker run --rm -v "$PWD:/workspace" aquasec/trivy:0.56.1
          fs --exit-code 0 --scanners vuln,secret,misconfig
          --format sarif --output /workspace/trivy.sarif /workspace
      - name: Upload SARIF
        if: always()
        continue-on-error: true
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: trivy.sarif
          ref: refs/heads/${{ matrix.ref }}
          sha: ${{ steps.resolve_ref.outputs.sha }}
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: siegepal-trivy-results-${{ matrix.ref }}
          path: trivy.sarif
      - name: SiegePal gate — evaluate findings
        id: siegepal_gate
        if: always()
        env:
          SIEGEPAL_GATE_SEVERITY: "high"
          SIEGEPAL_SARIF_FILE: "trivy.sarif"
        run: |
          python3 - <<'PYEOF'
          import json, os
          order = ["none", "low", "medium", "high", "critical"]
          threshold = os.environ.get("SIEGEPAL_GATE_SEVERITY", "none").lower()
          sarif_file = os.environ.get("SIEGEPAL_SARIF_FILE", "")
          out = os.environ.get("GITHUB_OUTPUT")
          def emit(**kw):
              with open(out, "a") as fh:
                  for k, v in kw.items():
                      fh.write("%s=%s\n" % (k, v))
          def sev_from_result(r, rules):
              props = r.get("properties") or {}
              sc = props.get("security-severity")
              if sc is None:
                  rid = r.get("ruleId")
                  rule = rules.get(rid, {})
                  sc = ((rule.get("properties") or {}).get("security-severity"))
              if sc is not None:
                  try:
                      f = float(sc)
                  except (TypeError, ValueError):
                      f = -1
                  if f >= 9.0: return 4
                  if f >= 7.0: return 3
                  if f >= 4.0: return 2
                  if f > 0.0: return 1
              level = (r.get("level") or "").lower()
              return {"error": 3, "warning": 2, "note": 1, "none": 0}.get(level, 2)
          # A parseable SARIF file is the authoritative 'the scanner ran'
          # signal — most scanners exit non-zero when they FIND issues,
          # so the tool's exit code must NOT gate this (that would flag
          # every findings-present scan as an inconclusive crash).
          if not os.path.exists(sarif_file):
              # Missing output file is never "zero findings" (R5.5).
              emit(conclusion="neutral", findings="0",
                   summary="Tool did not produce an output file; result is inconclusive.")
              raise SystemExit(0)
          try:
              with open(sarif_file) as fh:
                  doc = json.load(fh)
          except Exception as exc:
              emit(conclusion="neutral", findings="0",
                   summary="Could not parse scanner output: %s" % exc)
              raise SystemExit(0)
          t = order.index(threshold) if threshold in order else 0
          count = 0
          for run in (doc.get("runs") or []):
              rules = {}
              for rule in (((run.get("tool") or {}).get("driver") or {}).get("rules") or []):
                  if rule.get("id"): rules[rule["id"]] = rule
              for res in (run.get("results") or []):
                  if sev_from_result(res, rules) >= t:
                      count += 1
          if count > 0:
              emit(conclusion="failure", findings=str(count),
                   summary="%d finding(s) at or above %s severity." % (count, threshold))
          else:
              emit(conclusion="success", findings="0",
                   summary="No findings at or above %s severity." % threshold)
          PYEOF
      - name: SiegePal gate — report check run
        if: always()
        continue-on-error: true
        uses: actions/github-script@v8
        env:
          SIEGEPAL_CHECK_CONCLUSION: "${{ steps.siegepal_gate.outputs.conclusion }}"
          SIEGEPAL_CHECK_SUMMARY: "${{ steps.siegepal_gate.outputs.summary }}"
          SIEGEPAL_HEAD_SHA: "${{ steps.resolve_ref.outputs.sha }}"
        with:
          script: |
            if (context.payload.pull_request && context.payload.pull_request.head.repo.fork) {
              core.info("Fork PR: GITHUB_TOKEN is read-only, skipping check run creation.");
              return;
            }
            const conclusion = process.env.SIEGEPAL_CHECK_CONCLUSION || "neutral";
            const advisory = 'This result is advisory. To block merges, mark this check ("SiegePal / Trivy") as a required status check in Settings → Branches → Branch protection rules.';
            const summary = (process.env.SIEGEPAL_CHECK_SUMMARY || "") + "\n\n" + advisory;
            const headSha = process.env.SIEGEPAL_HEAD_SHA || (context.payload.pull_request && context.payload.pull_request.head.sha) || context.sha;
            await github.rest.checks.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              name: 'SiegePal / Trivy',
              head_sha: headSha,
              status: "completed",
              conclusion: conclusion,
              output: { title: 'SiegePal / Trivy', summary: summary },
            });
      - name: SiegePal gate — enforce
        if: always()
        env:
          SIEGEPAL_CHECK_CONCLUSION: "${{ steps.siegepal_gate.outputs.conclusion }}"
        run: |
          if [ "${{ github.event.pull_request.head.repo.fork }}" = "true" ]; then
            echo "Fork PR: findings uploaded as artifacts; not failing the workflow."
            exit 0
          fi
          if [ "$SIEGEPAL_CHECK_CONCLUSION" = "failure" ]; then
            echo "SiegePal gate: blocking findings present."
            exit 1
          fi
          if [ "$SIEGEPAL_CHECK_CONCLUSION" = "neutral" ]; then
            echo "SiegePal gate: no conclusive output (nothing to scan or tool produced no report); not blocking."
            exit 0
          fi
          echo "SiegePal gate: passed."
  checkov:
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.checkov == true || inputs.checkov == 'true' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        ref: ${{ fromJSON(github.event_name != 'schedule' && format('["{0}"]', github.ref_name) || '["main"]') }}
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ matrix.ref }}
      - name: Resolve scanned ref
        id: resolve_ref
        run: |
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install Checkov
        run: pip install --disable-pip-version-check --no-cache-dir checkov
      - name: Run Checkov
        id: scan
        continue-on-error: true
        run: checkov -d . -o sarif --output-file-path console,checkov.sarif --soft-fail
      - name: Upload SARIF
        if: always()
        continue-on-error: true
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: checkov.sarif
          ref: refs/heads/${{ matrix.ref }}
          sha: ${{ steps.resolve_ref.outputs.sha }}
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: siegepal-checkov-results-${{ matrix.ref }}
          path: checkov.sarif
      - name: SiegePal gate — evaluate findings
        id: siegepal_gate
        if: always()
        env:
          SIEGEPAL_GATE_SEVERITY: "high"
          SIEGEPAL_SARIF_FILE: "checkov.sarif"
        run: |
          python3 - <<'PYEOF'
          import json, os
          order = ["none", "low", "medium", "high", "critical"]
          threshold = os.environ.get("SIEGEPAL_GATE_SEVERITY", "none").lower()
          sarif_file = os.environ.get("SIEGEPAL_SARIF_FILE", "")
          out = os.environ.get("GITHUB_OUTPUT")
          def emit(**kw):
              with open(out, "a") as fh:
                  for k, v in kw.items():
                      fh.write("%s=%s\n" % (k, v))
          def sev_from_result(r, rules):
              props = r.get("properties") or {}
              sc = props.get("security-severity")
              if sc is None:
                  rid = r.get("ruleId")
                  rule = rules.get(rid, {})
                  sc = ((rule.get("properties") or {}).get("security-severity"))
              if sc is not None:
                  try:
                      f = float(sc)
                  except (TypeError, ValueError):
                      f = -1
                  if f >= 9.0: return 4
                  if f >= 7.0: return 3
                  if f >= 4.0: return 2
                  if f > 0.0: return 1
              level = (r.get("level") or "").lower()
              return {"error": 3, "warning": 2, "note": 1, "none": 0}.get(level, 2)
          # A parseable SARIF file is the authoritative 'the scanner ran'
          # signal — most scanners exit non-zero when they FIND issues,
          # so the tool's exit code must NOT gate this (that would flag
          # every findings-present scan as an inconclusive crash).
          if not os.path.exists(sarif_file):
              # Missing output file is never "zero findings" (R5.5).
              emit(conclusion="neutral", findings="0",
                   summary="Tool did not produce an output file; result is inconclusive.")
              raise SystemExit(0)
          try:
              with open(sarif_file) as fh:
                  doc = json.load(fh)
          except Exception as exc:
              emit(conclusion="neutral", findings="0",
                   summary="Could not parse scanner output: %s" % exc)
              raise SystemExit(0)
          t = order.index(threshold) if threshold in order else 0
          count = 0
          for run in (doc.get("runs") or []):
              rules = {}
              for rule in (((run.get("tool") or {}).get("driver") or {}).get("rules") or []):
                  if rule.get("id"): rules[rule["id"]] = rule
              for res in (run.get("results") or []):
                  if sev_from_result(res, rules) >= t:
                      count += 1
          if count > 0:
              emit(conclusion="failure", findings=str(count),
                   summary="%d finding(s) at or above %s severity." % (count, threshold))
          else:
              emit(conclusion="success", findings="0",
                   summary="No findings at or above %s severity." % threshold)
          PYEOF
      - name: SiegePal gate — report check run
        if: always()
        continue-on-error: true
        uses: actions/github-script@v8
        env:
          SIEGEPAL_CHECK_CONCLUSION: "${{ steps.siegepal_gate.outputs.conclusion }}"
          SIEGEPAL_CHECK_SUMMARY: "${{ steps.siegepal_gate.outputs.summary }}"
          SIEGEPAL_HEAD_SHA: "${{ steps.resolve_ref.outputs.sha }}"
        with:
          script: |
            if (context.payload.pull_request && context.payload.pull_request.head.repo.fork) {
              core.info("Fork PR: GITHUB_TOKEN is read-only, skipping check run creation.");
              return;
            }
            const conclusion = process.env.SIEGEPAL_CHECK_CONCLUSION || "neutral";
            const advisory = 'This result is advisory. To block merges, mark this check ("SiegePal / Checkov") as a required status check in Settings → Branches → Branch protection rules.';
            const summary = (process.env.SIEGEPAL_CHECK_SUMMARY || "") + "\n\n" + advisory;
            const headSha = process.env.SIEGEPAL_HEAD_SHA || (context.payload.pull_request && context.payload.pull_request.head.sha) || context.sha;
            await github.rest.checks.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              name: 'SiegePal / Checkov',
              head_sha: headSha,
              status: "completed",
              conclusion: conclusion,
              output: { title: 'SiegePal / Checkov', summary: summary },
            });
      - name: SiegePal gate — enforce
        if: always()
        env:
          SIEGEPAL_CHECK_CONCLUSION: "${{ steps.siegepal_gate.outputs.conclusion }}"
        run: |
          if [ "${{ github.event.pull_request.head.repo.fork }}" = "true" ]; then
            echo "Fork PR: findings uploaded as artifacts; not failing the workflow."
            exit 0
          fi
          if [ "$SIEGEPAL_CHECK_CONCLUSION" = "failure" ]; then
            echo "SiegePal gate: blocking findings present."
            exit 1
          fi
          if [ "$SIEGEPAL_CHECK_CONCLUSION" = "neutral" ]; then
            echo "SiegePal gate: no conclusive output (nothing to scan or tool produced no report); not blocking."
            exit 0
          fi
          echo "SiegePal gate: passed."
  bandit:
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.bandit == true || inputs.bandit == 'true' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        ref: ${{ fromJSON(github.event_name != 'schedule' && format('["{0}"]', github.ref_name) || '["main"]') }}
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ matrix.ref }}
      - name: Resolve scanned ref
        id: resolve_ref
        run: |
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install Bandit
        run: pip install --disable-pip-version-check --no-cache-dir "bandit[sarif]"
      - name: Run Bandit
        id: scan
        continue-on-error: true
        run: bandit -r . -f sarif -o bandit.sarif || true
      - name: Upload SARIF
        if: always()
        continue-on-error: true
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: bandit.sarif
          ref: refs/heads/${{ matrix.ref }}
          sha: ${{ steps.resolve_ref.outputs.sha }}
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: siegepal-bandit-results-${{ matrix.ref }}
          path: bandit.sarif
      - name: SiegePal gate — evaluate findings
        id: siegepal_gate
        if: always()
        env:
          SIEGEPAL_GATE_SEVERITY: "high"
          SIEGEPAL_SARIF_FILE: "bandit.sarif"
        run: |
          python3 - <<'PYEOF'
          import json, os
          order = ["none", "low", "medium", "high", "critical"]
          threshold = os.environ.get("SIEGEPAL_GATE_SEVERITY", "none").lower()
          sarif_file = os.environ.get("SIEGEPAL_SARIF_FILE", "")
          out = os.environ.get("GITHUB_OUTPUT")
          def emit(**kw):
              with open(out, "a") as fh:
                  for k, v in kw.items():
                      fh.write("%s=%s\n" % (k, v))
          def sev_from_result(r, rules):
              props = r.get("properties") or {}
              sc = props.get("security-severity")
              if sc is None:
                  rid = r.get("ruleId")
                  rule = rules.get(rid, {})
                  sc = ((rule.get("properties") or {}).get("security-severity"))
              if sc is not None:
                  try:
                      f = float(sc)
                  except (TypeError, ValueError):
                      f = -1
                  if f >= 9.0: return 4
                  if f >= 7.0: return 3
                  if f >= 4.0: return 2
                  if f > 0.0: return 1
              level = (r.get("level") or "").lower()
              return {"error": 3, "warning": 2, "note": 1, "none": 0}.get(level, 2)
          # A parseable SARIF file is the authoritative 'the scanner ran'
          # signal — most scanners exit non-zero when they FIND issues,
          # so the tool's exit code must NOT gate this (that would flag
          # every findings-present scan as an inconclusive crash).
          if not os.path.exists(sarif_file):
              # Missing output file is never "zero findings" (R5.5).
              emit(conclusion="neutral", findings="0",
                   summary="Tool did not produce an output file; result is inconclusive.")
              raise SystemExit(0)
          try:
              with open(sarif_file) as fh:
                  doc = json.load(fh)
          except Exception as exc:
              emit(conclusion="neutral", findings="0",
                   summary="Could not parse scanner output: %s" % exc)
              raise SystemExit(0)
          t = order.index(threshold) if threshold in order else 0
          count = 0
          for run in (doc.get("runs") or []):
              rules = {}
              for rule in (((run.get("tool") or {}).get("driver") or {}).get("rules") or []):
                  if rule.get("id"): rules[rule["id"]] = rule
              for res in (run.get("results") or []):
                  if sev_from_result(res, rules) >= t:
                      count += 1
          if count > 0:
              emit(conclusion="failure", findings=str(count),
                   summary="%d finding(s) at or above %s severity." % (count, threshold))
          else:
              emit(conclusion="success", findings="0",
                   summary="No findings at or above %s severity." % threshold)
          PYEOF
      - name: SiegePal gate — report check run
        if: always()
        continue-on-error: true
        uses: actions/github-script@v8
        env:
          SIEGEPAL_CHECK_CONCLUSION: "${{ steps.siegepal_gate.outputs.conclusion }}"
          SIEGEPAL_CHECK_SUMMARY: "${{ steps.siegepal_gate.outputs.summary }}"
          SIEGEPAL_HEAD_SHA: "${{ steps.resolve_ref.outputs.sha }}"
        with:
          script: |
            if (context.payload.pull_request && context.payload.pull_request.head.repo.fork) {
              core.info("Fork PR: GITHUB_TOKEN is read-only, skipping check run creation.");
              return;
            }
            const conclusion = process.env.SIEGEPAL_CHECK_CONCLUSION || "neutral";
            const advisory = 'This result is advisory. To block merges, mark this check ("SiegePal / Bandit") as a required status check in Settings → Branches → Branch protection rules.';
            const summary = (process.env.SIEGEPAL_CHECK_SUMMARY || "") + "\n\n" + advisory;
            const headSha = process.env.SIEGEPAL_HEAD_SHA || (context.payload.pull_request && context.payload.pull_request.head.sha) || context.sha;
            await github.rest.checks.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              name: 'SiegePal / Bandit',
              head_sha: headSha,
              status: "completed",
              conclusion: conclusion,
              output: { title: 'SiegePal / Bandit', summary: summary },
            });
      - name: SiegePal gate — enforce
        if: always()
        env:
          SIEGEPAL_CHECK_CONCLUSION: "${{ steps.siegepal_gate.outputs.conclusion }}"
        run: |
          if [ "${{ github.event.pull_request.head.repo.fork }}" = "true" ]; then
            echo "Fork PR: findings uploaded as artifacts; not failing the workflow."
            exit 0
          fi
          if [ "$SIEGEPAL_CHECK_CONCLUSION" = "failure" ]; then
            echo "SiegePal gate: blocking findings present."
            exit 1
          fi
          if [ "$SIEGEPAL_CHECK_CONCLUSION" = "neutral" ]; then
            echo "SiegePal gate: no conclusive output (nothing to scan or tool produced no report); not blocking."
            exit 0
          fi
          echo "SiegePal gate: passed."
  snyk:
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.snyk == true || inputs.snyk == 'true' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        ref: ${{ fromJSON(github.event_name != 'schedule' && format('["{0}"]', github.ref_name) || '["main"]') }}
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ matrix.ref }}
      - name: Resolve scanned ref
        id: resolve_ref
        run: |
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
      - name: Install Snyk CLI
        run: npm install -g snyk
      - name: Run Snyk Open Source (SCA)
        id: scan
        continue-on-error: true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        run: snyk test --sarif-file-output=snyk.sarif || true
      - name: Upload SARIF
        if: always()
        continue-on-error: true
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: snyk.sarif
          ref: refs/heads/${{ matrix.ref }}
          sha: ${{ steps.resolve_ref.outputs.sha }}
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: siegepal-snyk-results-${{ matrix.ref }}
          path: snyk.sarif
      - name: SiegePal gate — evaluate findings
        id: siegepal_gate
        if: always()
        env:
          SIEGEPAL_GATE_SEVERITY: "high"
          SIEGEPAL_SARIF_FILE: "snyk.sarif"
        run: |
          python3 - <<'PYEOF'
          import json, os
          order = ["none", "low", "medium", "high", "critical"]
          threshold = os.environ.get("SIEGEPAL_GATE_SEVERITY", "none").lower()
          sarif_file = os.environ.get("SIEGEPAL_SARIF_FILE", "")
          out = os.environ.get("GITHUB_OUTPUT")
          def emit(**kw):
              with open(out, "a") as fh:
                  for k, v in kw.items():
                      fh.write("%s=%s\n" % (k, v))
          def sev_from_result(r, rules):
              props = r.get("properties") or {}
              sc = props.get("security-severity")
              if sc is None:
                  rid = r.get("ruleId")
                  rule = rules.get(rid, {})
                  sc = ((rule.get("properties") or {}).get("security-severity"))
              if sc is not None:
                  try:
                      f = float(sc)
                  except (TypeError, ValueError):
                      f = -1
                  if f >= 9.0: return 4
                  if f >= 7.0: return 3
                  if f >= 4.0: return 2
                  if f > 0.0: return 1
              level = (r.get("level") or "").lower()
              return {"error": 3, "warning": 2, "note": 1, "none": 0}.get(level, 2)
          # A parseable SARIF file is the authoritative 'the scanner ran'
          # signal — most scanners exit non-zero when they FIND issues,
          # so the tool's exit code must NOT gate this (that would flag
          # every findings-present scan as an inconclusive crash).
          if not os.path.exists(sarif_file):
              # Missing output file is never "zero findings" (R5.5).
              emit(conclusion="neutral", findings="0",
                   summary="Tool did not produce an output file; result is inconclusive.")
              raise SystemExit(0)
          try:
              with open(sarif_file) as fh:
                  doc = json.load(fh)
          except Exception as exc:
              emit(conclusion="neutral", findings="0",
                   summary="Could not parse scanner output: %s" % exc)
              raise SystemExit(0)
          t = order.index(threshold) if threshold in order else 0
          count = 0
          for run in (doc.get("runs") or []):
              rules = {}
              for rule in (((run.get("tool") or {}).get("driver") or {}).get("rules") or []):
                  if rule.get("id"): rules[rule["id"]] = rule
              for res in (run.get("results") or []):
                  if sev_from_result(res, rules) >= t:
                      count += 1
          if count > 0:
              emit(conclusion="failure", findings=str(count),
                   summary="%d finding(s) at or above %s severity." % (count, threshold))
          else:
              emit(conclusion="success", findings="0",
                   summary="No findings at or above %s severity." % threshold)
          PYEOF
      - name: SiegePal gate — report check run
        if: always()
        continue-on-error: true
        uses: actions/github-script@v8
        env:
          SIEGEPAL_CHECK_CONCLUSION: "${{ steps.siegepal_gate.outputs.conclusion }}"
          SIEGEPAL_CHECK_SUMMARY: "${{ steps.siegepal_gate.outputs.summary }}"
          SIEGEPAL_HEAD_SHA: "${{ steps.resolve_ref.outputs.sha }}"
        with:
          script: |
            if (context.payload.pull_request && context.payload.pull_request.head.repo.fork) {
              core.info("Fork PR: GITHUB_TOKEN is read-only, skipping check run creation.");
              return;
            }
            const conclusion = process.env.SIEGEPAL_CHECK_CONCLUSION || "neutral";
            const advisory = 'This result is advisory. To block merges, mark this check ("SiegePal / Snyk (SCA)") as a required status check in Settings → Branches → Branch protection rules.';
            const summary = (process.env.SIEGEPAL_CHECK_SUMMARY || "") + "\n\n" + advisory;
            const headSha = process.env.SIEGEPAL_HEAD_SHA || (context.payload.pull_request && context.payload.pull_request.head.sha) || context.sha;
            await github.rest.checks.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              name: 'SiegePal / Snyk (SCA)',
              head_sha: headSha,
              status: "completed",
              conclusion: conclusion,
              output: { title: 'SiegePal / Snyk (SCA)', summary: summary },
            });
      - name: SiegePal gate — enforce
        if: always()
        env:
          SIEGEPAL_CHECK_CONCLUSION: "${{ steps.siegepal_gate.outputs.conclusion }}"
        run: |
          if [ "${{ github.event.pull_request.head.repo.fork }}" = "true" ]; then
            echo "Fork PR: findings uploaded as artifacts; not failing the workflow."
            exit 0
          fi
          if [ "$SIEGEPAL_CHECK_CONCLUSION" = "failure" ]; then
            echo "SiegePal gate: blocking findings present."
            exit 1
          fi
          if [ "$SIEGEPAL_CHECK_CONCLUSION" = "neutral" ]; then
            echo "SiegePal gate: no conclusive output (nothing to scan or tool produced no report); not blocking."
            exit 0
          fi
          echo "SiegePal gate: passed."
  snyk-code:
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.snyk_code == true || inputs.snyk_code == 'true' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        ref: ${{ fromJSON(github.event_name != 'schedule' && format('["{0}"]', github.ref_name) || '["main"]') }}
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ matrix.ref }}
      - name: Resolve scanned ref
        id: resolve_ref
        run: |
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
      - name: Install Snyk CLI
        run: npm install -g snyk
      - name: Run Snyk Code (SAST)
        id: scan
        continue-on-error: true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        run: snyk code test --sarif-file-output=snyk-code.sarif || true
      - name: Upload SARIF
        if: always()
        continue-on-error: true
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: snyk-code.sarif
          ref: refs/heads/${{ matrix.ref }}
          sha: ${{ steps.resolve_ref.outputs.sha }}
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: siegepal-snyk_code-results-${{ matrix.ref }}
          path: snyk-code.sarif
      - name: SiegePal gate — evaluate findings
        id: siegepal_gate
        if: always()
        env:
          SIEGEPAL_GATE_SEVERITY: "high"
          SIEGEPAL_SARIF_FILE: "snyk-code.sarif"
        run: |
          python3 - <<'PYEOF'
          import json, os
          order = ["none", "low", "medium", "high", "critical"]
          threshold = os.environ.get("SIEGEPAL_GATE_SEVERITY", "none").lower()
          sarif_file = os.environ.get("SIEGEPAL_SARIF_FILE", "")
          out = os.environ.get("GITHUB_OUTPUT")
          def emit(**kw):
              with open(out, "a") as fh:
                  for k, v in kw.items():
                      fh.write("%s=%s\n" % (k, v))
          def sev_from_result(r, rules):
              props = r.get("properties") or {}
              sc = props.get("security-severity")
              if sc is None:
                  rid = r.get("ruleId")
                  rule = rules.get(rid, {})
                  sc = ((rule.get("properties") or {}).get("security-severity"))
              if sc is not None:
                  try:
                      f = float(sc)
                  except (TypeError, ValueError):
                      f = -1
                  if f >= 9.0: return 4
                  if f >= 7.0: return 3
                  if f >= 4.0: return 2
                  if f > 0.0: return 1
              level = (r.get("level") or "").lower()
              return {"error": 3, "warning": 2, "note": 1, "none": 0}.get(level, 2)
          # A parseable SARIF file is the authoritative 'the scanner ran'
          # signal — most scanners exit non-zero when they FIND issues,
          # so the tool's exit code must NOT gate this (that would flag
          # every findings-present scan as an inconclusive crash).
          if not os.path.exists(sarif_file):
              # Missing output file is never "zero findings" (R5.5).
              emit(conclusion="neutral", findings="0",
                   summary="Tool did not produce an output file; result is inconclusive.")
              raise SystemExit(0)
          try:
              with open(sarif_file) as fh:
                  doc = json.load(fh)
          except Exception as exc:
              emit(conclusion="neutral", findings="0",
                   summary="Could not parse scanner output: %s" % exc)
              raise SystemExit(0)
          t = order.index(threshold) if threshold in order else 0
          count = 0
          for run in (doc.get("runs") or []):
              rules = {}
              for rule in (((run.get("tool") or {}).get("driver") or {}).get("rules") or []):
                  if rule.get("id"): rules[rule["id"]] = rule
              for res in (run.get("results") or []):
                  if sev_from_result(res, rules) >= t:
                      count += 1
          if count > 0:
              emit(conclusion="failure", findings=str(count),
                   summary="%d finding(s) at or above %s severity." % (count, threshold))
          else:
              emit(conclusion="success", findings="0",
                   summary="No findings at or above %s severity." % threshold)
          PYEOF
      - name: SiegePal gate — report check run
        if: always()
        continue-on-error: true
        uses: actions/github-script@v8
        env:
          SIEGEPAL_CHECK_CONCLUSION: "${{ steps.siegepal_gate.outputs.conclusion }}"
          SIEGEPAL_CHECK_SUMMARY: "${{ steps.siegepal_gate.outputs.summary }}"
          SIEGEPAL_HEAD_SHA: "${{ steps.resolve_ref.outputs.sha }}"
        with:
          script: |
            if (context.payload.pull_request && context.payload.pull_request.head.repo.fork) {
              core.info("Fork PR: GITHUB_TOKEN is read-only, skipping check run creation.");
              return;
            }
            const conclusion = process.env.SIEGEPAL_CHECK_CONCLUSION || "neutral";
            const advisory = 'This result is advisory. To block merges, mark this check ("SiegePal / Snyk Code") as a required status check in Settings → Branches → Branch protection rules.';
            const summary = (process.env.SIEGEPAL_CHECK_SUMMARY || "") + "\n\n" + advisory;
            const headSha = process.env.SIEGEPAL_HEAD_SHA || (context.payload.pull_request && context.payload.pull_request.head.sha) || context.sha;
            await github.rest.checks.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              name: 'SiegePal / Snyk Code',
              head_sha: headSha,
              status: "completed",
              conclusion: conclusion,
              output: { title: 'SiegePal / Snyk Code', summary: summary },
            });
      - name: SiegePal gate — enforce
        if: always()
        env:
          SIEGEPAL_CHECK_CONCLUSION: "${{ steps.siegepal_gate.outputs.conclusion }}"
        run: |
          if [ "${{ github.event.pull_request.head.repo.fork }}" = "true" ]; then
            echo "Fork PR: findings uploaded as artifacts; not failing the workflow."
            exit 0
          fi
          if [ "$SIEGEPAL_CHECK_CONCLUSION" = "failure" ]; then
            echo "SiegePal gate: blocking findings present."
            exit 1
          fi
          if [ "$SIEGEPAL_CHECK_CONCLUSION" = "neutral" ]; then
            echo "SiegePal gate: no conclusive output (nothing to scan or tool produced no report); not blocking."
            exit 0
          fi
          echo "SiegePal gate: passed."
  snyk-iac:
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.snyk_iac == true || inputs.snyk_iac == 'true' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        ref: ${{ fromJSON(github.event_name != 'schedule' && format('["{0}"]', github.ref_name) || '["main"]') }}
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ matrix.ref }}
      - name: Resolve scanned ref
        id: resolve_ref
        run: |
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
      - name: Install Snyk CLI
        run: npm install -g snyk
      - name: Run Snyk IaC
        id: scan
        continue-on-error: true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        run: snyk iac test --sarif-file-output=snyk-iac.sarif || true
      - name: Upload SARIF
        if: always()
        continue-on-error: true
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: snyk-iac.sarif
          ref: refs/heads/${{ matrix.ref }}
          sha: ${{ steps.resolve_ref.outputs.sha }}
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: siegepal-snyk_iac-results-${{ matrix.ref }}
          path: snyk-iac.sarif
      - name: SiegePal gate — evaluate findings
        id: siegepal_gate
        if: always()
        env:
          SIEGEPAL_GATE_SEVERITY: "high"
          SIEGEPAL_SARIF_FILE: "snyk-iac.sarif"
        run: |
          python3 - <<'PYEOF'
          import json, os
          order = ["none", "low", "medium", "high", "critical"]
          threshold = os.environ.get("SIEGEPAL_GATE_SEVERITY", "none").lower()
          sarif_file = os.environ.get("SIEGEPAL_SARIF_FILE", "")
          out = os.environ.get("GITHUB_OUTPUT")
          def emit(**kw):
              with open(out, "a") as fh:
                  for k, v in kw.items():
                      fh.write("%s=%s\n" % (k, v))
          def sev_from_result(r, rules):
              props = r.get("properties") or {}
              sc = props.get("security-severity")
              if sc is None:
                  rid = r.get("ruleId")
                  rule = rules.get(rid, {})
                  sc = ((rule.get("properties") or {}).get("security-severity"))
              if sc is not None:
                  try:
                      f = float(sc)
                  except (TypeError, ValueError):
                      f = -1
                  if f >= 9.0: return 4
                  if f >= 7.0: return 3
                  if f >= 4.0: return 2
                  if f > 0.0: return 1
              level = (r.get("level") or "").lower()
              return {"error": 3, "warning": 2, "note": 1, "none": 0}.get(level, 2)
          # A parseable SARIF file is the authoritative 'the scanner ran'
          # signal — most scanners exit non-zero when they FIND issues,
          # so the tool's exit code must NOT gate this (that would flag
          # every findings-present scan as an inconclusive crash).
          if not os.path.exists(sarif_file):
              # Missing output file is never "zero findings" (R5.5).
              emit(conclusion="neutral", findings="0",
                   summary="Tool did not produce an output file; result is inconclusive.")
              raise SystemExit(0)
          try:
              with open(sarif_file) as fh:
                  doc = json.load(fh)
          except Exception as exc:
              emit(conclusion="neutral", findings="0",
                   summary="Could not parse scanner output: %s" % exc)
              raise SystemExit(0)
          t = order.index(threshold) if threshold in order else 0
          count = 0
          for run in (doc.get("runs") or []):
              rules = {}
              for rule in (((run.get("tool") or {}).get("driver") or {}).get("rules") or []):
                  if rule.get("id"): rules[rule["id"]] = rule
              for res in (run.get("results") or []):
                  if sev_from_result(res, rules) >= t:
                      count += 1
          if count > 0:
              emit(conclusion="failure", findings=str(count),
                   summary="%d finding(s) at or above %s severity." % (count, threshold))
          else:
              emit(conclusion="success", findings="0",
                   summary="No findings at or above %s severity." % threshold)
          PYEOF
      - name: SiegePal gate — report check run
        if: always()
        continue-on-error: true
        uses: actions/github-script@v8
        env:
          SIEGEPAL_CHECK_CONCLUSION: "${{ steps.siegepal_gate.outputs.conclusion }}"
          SIEGEPAL_CHECK_SUMMARY: "${{ steps.siegepal_gate.outputs.summary }}"
          SIEGEPAL_HEAD_SHA: "${{ steps.resolve_ref.outputs.sha }}"
        with:
          script: |
            if (context.payload.pull_request && context.payload.pull_request.head.repo.fork) {
              core.info("Fork PR: GITHUB_TOKEN is read-only, skipping check run creation.");
              return;
            }
            const conclusion = process.env.SIEGEPAL_CHECK_CONCLUSION || "neutral";
            const advisory = 'This result is advisory. To block merges, mark this check ("SiegePal / Snyk IaC") as a required status check in Settings → Branches → Branch protection rules.';
            const summary = (process.env.SIEGEPAL_CHECK_SUMMARY || "") + "\n\n" + advisory;
            const headSha = process.env.SIEGEPAL_HEAD_SHA || (context.payload.pull_request && context.payload.pull_request.head.sha) || context.sha;
            await github.rest.checks.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              name: 'SiegePal / Snyk IaC',
              head_sha: headSha,
              status: "completed",
              conclusion: conclusion,
              output: { title: 'SiegePal / Snyk IaC', summary: summary },
            });
      - name: SiegePal gate — enforce
        if: always()
        env:
          SIEGEPAL_CHECK_CONCLUSION: "${{ steps.siegepal_gate.outputs.conclusion }}"
        run: |
          if [ "${{ github.event.pull_request.head.repo.fork }}" = "true" ]; then
            echo "Fork PR: findings uploaded as artifacts; not failing the workflow."
            exit 0
          fi
          if [ "$SIEGEPAL_CHECK_CONCLUSION" = "failure" ]; then
            echo "SiegePal gate: blocking findings present."
            exit 1
          fi
          if [ "$SIEGEPAL_CHECK_CONCLUSION" = "neutral" ]; then
            echo "SiegePal gate: no conclusive output (nothing to scan or tool produced no report); not blocking."
            exit 0
          fi
          echo "SiegePal gate: passed."
